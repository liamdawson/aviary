---
- name: install usual tools
  hosts: localhost
  tags:
    - software
  roles:
    - tools
    - fedora/homebrew
    - gnupg_ssh

- name: system configuration
  hosts: localhost
  tags:
    - cinnamon
  roles:
    - vanity/cinnamon_settings
